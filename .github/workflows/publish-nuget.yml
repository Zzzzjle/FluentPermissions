name: Publish to NuGet

on:
  push:
    branches:
      - main

jobs:
  pack-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            9.0.x

      - name: Compute pre-release version suffix
        id: vars
        shell: bash
        run: |
          suffix="preview.1.$(date +%y%m%d%H%M)"
          echo "version_suffix=$suffix" >> "$GITHUB_OUTPUT"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-restore

      - name: Test
        run: dotnet test -c Release --no-build

      - name: Pack FluentPermissions.Core
        run: |
          mkdir -p out
          dotnet pack FluentPermissions/FluentPermissions.Core/FluentPermissions.Core.csproj \
            -c Release -o ./out \
            /p:ContinuousIntegrationBuild=true \
            --version-suffix "${{ steps.vars.outputs.version_suffix }}"

      - name: Pack FluentPermissions analyzer
        run: |
          dotnet pack FluentPermissions/FluentPermissions/FluentPermissions.csproj \
            -c Release -o ./out \
            /p:ContinuousIntegrationBuild=true \
            --version-suffix "${{ steps.vars.outputs.version_suffix }}"

      - name: Publish to NuGet.org
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          dotnet nuget push "out/*.nupkg" --api-key "$NUGET_API_KEY" --source "https://api.nuget.org/v3/index.json" --skip-duplicate
